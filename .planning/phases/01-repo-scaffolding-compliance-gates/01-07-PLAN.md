---
phase: 01-repo-scaffolding-compliance-gates
plan: 07
type: execute
wave: 6
depends_on: ["01-06"]
files_modified:
  - examples/basic-usage/package.json
  - examples/basic-usage/vite.config.ts
  - examples/basic-usage/index.html
  - examples/basic-usage/src/main.tsx
autonomous: true
requirements: [INFRA-01, INFRA-07]

must_haves:
  truths:
    - "examples/basic-usage installs @gruporeacciona/react-knowledge-graph as a real workspace:* dependency (not a relative import) and `vite build` succeeds — this is the integration-proof slice per RESEARCH.md Slice 5 (INFRA-01)."
    - "`pnpm why react` and `pnpm why three`, run from repo root after installing examples/basic-usage, each show exactly one resolved version in the dependency tree — no duplicate React/Three instances (INFRA-07, roadmap Success Criterion #5, covered per specless-probe edge item)."
  artifacts:
    - examples/basic-usage/package.json
    - examples/basic-usage/vite.config.ts
    - examples/basic-usage/index.html
    - examples/basic-usage/src/main.tsx
  key_links:
    - "examples/basic-usage/package.json's dependency on @gruporeacciona/react-knowledge-graph (workspace:*) is the first real, non-scaffolding consumer of the entire package graph built in Plans 01-03..01-06"
---

<objective>
Create a minimal, real (not config-less) Vite skeleton under `examples/basic-usage` per D-11, installing `@gruporeacciona/react-knowledge-graph` as an actual `workspace:*` dependency — this is RESEARCH.md's Slice 5, the integration-proof step that makes INFRA-07's "no duplicate React/Three instances" claim empirically verifiable rather than theoretical. No mock graph data or real component rendering yet (that's Phase 3 / VIEWER-02, D-11) — just a working, buildable Vite app that resolves the full workspace dependency graph.

Purpose: Prove roadmap Success Criterion #5 end-to-end: install the published-shape package into a real consuming project and confirm no duplicate peer-dependency instances resolve.
Output: `examples/basic-usage/package.json`, `examples/basic-usage/vite.config.ts`, `examples/basic-usage/index.html`, `examples/basic-usage/src/main.tsx`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-CONTEXT.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-RESEARCH.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-PATTERNS.md
@examples/basic-usage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold examples/basic-usage as a real (if minimal) Vite project</name>
  <files>examples/basic-usage/package.json, examples/basic-usage/vite.config.ts, examples/basic-usage/index.html, examples/basic-usage/src/main.tsx</files>
  <read_first>
    - 01-PATTERNS.md lines 321-327 ("examples/* skeleton (D-11)" — exact guidance: wrap the existing basic-usage.tsx snippet's shape inside a minimal real Vite project)
    - examples/basic-usage.tsx (existing illustrative snippet, already in context — the one file in this phase with a genuine in-repo content analog: exact import path, `kind:identifier` node-ID convention, props shape)
    - 01-RESEARCH.md Open Question #3 (a minimal but real Vite config is safer than a config-less stub)
    - 01-CONTEXT.md line 40 (D-11: skeleton only, no app functionality yet — completed with mock data in Phase 3)
  </read_first>
  <action>
    Create `examples/basic-usage/package.json`: `"name": "@gruporeacciona/example-basic-usage"`, `"private": true`, `"type": "module"`, `"version": "0.0.0"`, scripts `"dev": "vite"`, `"build": "vite build"`, `"dependencies": { "@gruporeacciona/react-knowledge-graph": "workspace:*", "react": "catalog:", "react-dom": "catalog:" }`, and `"devDependencies"` for `vite` and `@vitejs/plugin-react` (pick current stable versions at implementation time via `npm view vite version` / `npm view @vitejs/plugin-react version`, since RESEARCH.md did not pin these — flag this as Claude's Discretion in the SUMMARY).

    Create `examples/basic-usage/vite.config.ts` with the standard `@vitejs/plugin-react` setup (`defineConfig({ plugins: [react()] })`) — minimal, no custom aliasing needed since pnpm workspace linking already resolves `@gruporeacciona/react-knowledge-graph`.

    Create `examples/basic-usage/index.html` with a minimal HTML shell (a single `<div id="root">` and a `<script type="module" src="/src/main.tsx">` tag) — standard Vite entry shape.

    Create `examples/basic-usage/src/main.tsx` that imports `@gruporeacciona/react-knowledge-graph`'s current placeholder export (from Plan 01-06 — NOT the `KnowledgeGraphViewer` component itself, since it doesn't exist until Phase 3) and mounts a trivial placeholder React tree via `ReactDOM.createRoot`. Do NOT reproduce `examples/basic-usage.tsx`'s full mock-data/`KnowledgeGraphViewer` usage yet — that snippet becomes the real Phase 3 deliverable (D-11); this task only proves the workspace/build/dependency-resolution chain works end-to-end. Note this deferral explicitly as a comment in `main.tsx` referencing VIEWER-02.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @gruporeacciona/example-basic-usage exec vite build` (or `turbo run build --filter=@gruporeacciona/example-basic-usage` if a `build` script is wired into turbo.json's task graph) exits 0.
    - `cat examples/basic-usage/package.json` shows `"@gruporeacciona/react-knowledge-graph": "workspace:*"` under `dependencies`.
    - `examples/basic-usage/src/main.tsx` does NOT contain a `KnowledgeGraphViewer` import (that component doesn't exist until Phase 3).
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm --filter @gruporeacciona/example-basic-usage exec vite build && ! grep -q "KnowledgeGraphViewer" examples/basic-usage/src/main.tsx</automated>
  </verify>
  <done>examples/basic-usage is a real, buildable Vite project installing react-knowledge-graph as a workspace dependency, without prematurely building Phase 3's actual viewer usage.</done>
</task>

<task type="auto">
  <name>Task 2: Empirically prove no duplicate React/Three instances (INFRA-07, roadmap Success Criterion #5)</name>
  <files>none (verification only)</files>
  <read_first>
    - 01-RESEARCH.md lines 418-420 (Pitfall 6 — the exact failure mode this task proves is absent)
  </read_first>
  <action>
    From the repo root, run `pnpm why react` and `pnpm why three` (after `pnpm install` has linked `examples/basic-usage`'s new dependency). Confirm each command's output shows exactly ONE resolved version of the package across the entire workspace dependency tree — not two different versions satisfied by different packages. If pnpm's strict, non-hoisted `node_modules` reveals more than one resolved version, this indicates a peerDependency vs. dependency mismatch in an earlier plan (01-04 or 01-06) that must be fixed before this task can pass — do not mask a real duplicate-instance finding by rerunning until it looks clean; escalate and fix the root cause package.json instead.
  </action>
  <acceptance_criteria>
    - `pnpm why react` output lists exactly one resolved version string for `react`.
    - `pnpm why three` output lists exactly one resolved version string for `three`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm why react 2>&1 | grep -c "^react " | grep -qx "1" && pnpm why three 2>&1 | grep -c "^three " | grep -qx "1"</automated>
  </verify>
  <done>Both `pnpm why react` and `pnpm why three` confirm a single resolved version across the whole workspace, empirically closing INFRA-07 / roadmap Success Criterion #5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| examples/basic-usage (first real consumer) ⇄ react-knowledge-graph | The first point where the whole package graph is exercised as a genuine external-shaped consumer would exercise it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Tampering | Duplicate React/Three instance surfacing only at real-consumption time | high | mitigate | Task 2's empirical `pnpm why` check — this is the exact scenario RESEARCH.md Pitfall 6 warns cannot be caught until a real consumer exists, which is why this slice exists |
</threat_model>

<verification>
- `vite build` succeeds for examples/basic-usage.
- `pnpm why react` / `pnpm why three` each show exactly one resolved version.
</verification>

<success_criteria>
A real (if minimal) consuming project builds successfully against the full workspace dependency graph with zero duplicate React/Three instances — the last piece of evidence needed before Plan 01-08's full-phase verification.
</success_criteria>

## Artifacts this phase produces

- `examples/basic-usage/package.json` — real workspace consumer of react-knowledge-graph
- `examples/basic-usage/vite.config.ts` — minimal Vite + React plugin config
- `examples/basic-usage/index.html` — Vite entry shell
- `examples/basic-usage/src/main.tsx` — placeholder mount point (no KnowledgeGraphViewer usage yet, D-11)

<output>
Create `.planning/phases/01-repo-scaffolding-compliance-gates/01-07-SUMMARY.md` when done
</output>
