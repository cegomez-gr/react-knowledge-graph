---
phase: 01-repo-scaffolding-compliance-gates
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/graph-core/package.json
  - packages/graph-core/tsconfig.json
  - packages/graph-core/src/index.ts
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03]

must_haves:
  truths:
    - "packages/graph-core/package.json declares zero dependencies on react/react-dom/three/@react-three/* (INFRA-01/D-05); `turbo run build --filter=@gruporeacciona/graph-core` and `turbo run typecheck --filter=@gruporeacciona/graph-core` both succeed via TypeScript project references (INFRA-02)."
    - "A throwaway disallowed import (react, three, a bare fetch(...) call) added to packages/graph-core/src/index.ts and linted makes `eslint` fail with the D-05 rule; removing it makes eslint pass again — empirically proves the Plan 01-02 guard is wired correctly for this package (INFRA-03)."
  artifacts:
    - packages/graph-core/package.json
    - packages/graph-core/tsconfig.json
    - packages/graph-core/src/index.ts
  key_links:
    - "packages/graph-core/tsconfig.json extends ../../tsconfig.base.json and has zero upstream project references (graph-core is the dependency root of the whole package graph per docs/03-architecture.md)"
    - "graph-core's package.json name (@gruporeacciona/graph-core) is the exact string later referenced as a workspace:* dependency by graph-renderer-three, react-knowledge-graph, and adapters/codebase-memory"
---

<objective>
Fully scaffold the `graph-core` package end-to-end (package.json, tsconfig.json, minimal buildable placeholder source) and empirically prove the D-05 architecture guard (Plan 01-02) actually fires against it — before any other package is added, per RESEARCH.md's Slice 1 sequencing. This is the dependency root of the entire package graph: `graph-core` must have zero React/Three/backend coupling, structurally enforced, not just documented.

Purpose: Make INFRA-01 (package exists in the workspace) and INFRA-02 (project references resolve) concretely true for the one package every other package in this monorepo depends on, and prove INFRA-03's lint guard is not just written but working.
Output: `packages/graph-core/package.json`, `packages/graph-core/tsconfig.json`, `packages/graph-core/src/index.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-CONTEXT.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-RESEARCH.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-PATTERNS.md
@./.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/graph-core</name>
  <files>packages/graph-core/package.json, packages/graph-core/tsconfig.json, packages/graph-core/src/index.ts</files>
  <read_first>
    - 01-PATTERNS.md lines 124-165 (tsconfig patterns — graph-core's tsconfig.json example: no "jsx", zero UI deps)
    - 01-PATTERNS.md lines 296-317 ("packages/<name>/package.json" pattern — naming/scope convention, D-12)
    - ./.claude/CLAUDE.md "Architectural / Layering Conventions" section (graph-core must have zero dependency on React or Three.js)
    - 01-RESEARCH.md lines 231-263 (Recommended Project Structure — graph-core's exact file layout)
  </read_first>
  <action>
    Create `packages/graph-core/package.json` with `"name": "@gruporeacciona/graph-core"` (D-12 scope), `"version": "0.0.0"`, `"private": true`, `"type": "module"`, `"main": "./dist/index.js"`, `"types": "./dist/index.d.ts"`, and scripts `"build": "tsc --build"`, `"lint": "eslint ."`, `"typecheck": "tsc --noEmit"`. Declare **zero** `dependencies`/`peerDependencies` on `react`, `react-dom`, `three`, or `@react-three/*` — this package must remain 100% framework/backend-agnostic per docs/03-architecture.md and the D-05 ESLint block already enforces this at the import level.

    Create `packages/graph-core/tsconfig.json` extending `../../tsconfig.base.json` (per PATTERNS.md's exact example): `compilerOptions: { outDir: "./dist", rootDir: "./src", tsBuildInfoFile: "./dist/.tsbuildinfo" }`, `include: ["src"]`. Do NOT set `"jsx"` (graph-core has no UI).

    Create `packages/graph-core/src/index.ts` containing only a placeholder export (e.g. a single comment noting real `GraphNode`/`GraphEdge`/`NormalizedGraph` types land in Phase 2, CORE-01) — enough for `tsc --build` to have a valid, non-empty compilation unit (an `include: ["src"]` config with zero files in `src/` errors with "No inputs were found"). No business logic belongs in this file; this is pure scaffolding per this phase's zero-application-code framing.
  </action>
  <acceptance_criteria>
    - `cat packages/graph-core/package.json` shows name `@gruporeacciona/graph-core` and no `react`/`react-dom`/`three`/`@react-three/*` key anywhere in the file.
    - `pnpm --filter @gruporeacciona/graph-core exec tsc --build` (or `turbo run build --filter=@gruporeacciona/graph-core`) exits 0.
    - `packages/graph-core/src/index.ts` exists and is non-empty.
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm exec turbo run build --filter=@gruporeacciona/graph-core && ! grep -E '"(react|react-dom|three|@react-three/[a-z-]+)"' packages/graph-core/package.json</automated>
  </verify>
  <done>packages/graph-core builds cleanly via Turborepo/tsc project references with zero React/Three/backend dependencies declared.</done>
</task>

<task type="auto">
  <name>Task 2: Empirically verify the D-05 architecture guard against graph-core</name>
  <files>packages/graph-core/src/index.ts (temporary edits only, reverted before completion)</files>
  <read_first>
    - eslint.config.js (created in Plan 01-02) — the D-05 block specifically
    - 01-PATTERNS.md lines 333-336 ("Verification step required before considering INFRA-03 done")
  </read_first>
  <action>
    Temporarily add a bare `import 'react';` line to `packages/graph-core/src/index.ts`, run `pnpm exec eslint packages/graph-core`, and confirm it exits non-zero with a `no-restricted-imports` violation naming `react`. Remove that line, then temporarily add a bare `fetch('http://example.com');` call, re-run eslint, and confirm it exits non-zero with a `no-restricted-globals` violation naming `fetch` (this is the exact mechanism RESEARCH.md Pitfall 1 warns a naive `no-restricted-imports`-only setup would miss). Remove that line too. Finally, run `pnpm exec eslint packages/graph-core` one more time on the clean file and confirm it exits 0 — do not leave any throwaway violation committed.
  </action>
  <acceptance_criteria>
    - The `react` import trial produces a non-zero eslint exit code with a message referencing `no-restricted-imports`.
    - The `fetch(...)` trial produces a non-zero eslint exit code with a message referencing `no-restricted-globals`.
    - The final, clean state of `packages/graph-core/src/index.ts` matches Task 1's placeholder content exactly (no leftover throwaway lines), and `pnpm exec eslint packages/graph-core` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint packages/graph-core</automated>
  </verify>
  <done>Both the react-import and fetch-global disallowed cases were empirically proven to fail lint, then reverted, leaving graph-core in a clean, lint-passing state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph-core source → downstream packages | Every other package in this monorepo transitively depends on graph-core; any coupling smuggled in here (React/Three/backend) propagates everywhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | packages/graph-core (dependency-direction regression) | high | mitigate | D-05 ESLint block (Plan 01-02) + this plan's empirical Task 2 verification, re-run any time graph-core's source changes in later phases |
</threat_model>

<verification>
- `turbo run build --filter=@gruporeacciona/graph-core` succeeds.
- `eslint packages/graph-core` passes clean and was empirically proven to fail on both a disallowed import and a disallowed global before being reverted.
</verification>

<success_criteria>
graph-core exists as a real, buildable, lint-clean TypeScript package with zero React/Three/backend coupling — structurally proven, not just documented — ready for graph-renderer-three, react-knowledge-graph, and adapters/codebase-memory to depend on it.
</success_criteria>

## Artifacts this phase produces

- `packages/graph-core/package.json` — `@gruporeacciona/graph-core`, zero UI/backend deps
- `packages/graph-core/tsconfig.json` — extends root base, composite, no jsx
- `packages/graph-core/src/index.ts` — placeholder entry point

<output>
Create `.planning/phases/01-repo-scaffolding-compliance-gates/01-03-SUMMARY.md` when done
</output>

