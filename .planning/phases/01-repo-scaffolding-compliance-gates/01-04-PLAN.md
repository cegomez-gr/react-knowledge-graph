---
phase: 01-repo-scaffolding-compliance-gates
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - packages/graph-renderer-three/package.json
  - packages/graph-renderer-three/tsconfig.json
  - packages/graph-renderer-three/src/index.ts
autonomous: false
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-07]

must_haves:
  truths:
    - "packages/graph-renderer-three/package.json declares react/react-dom/three/@react-three/fiber/@react-three/drei as peerDependencies (not plain dependencies) via the catalog: protocol (INFRA-07) — plain-dependency declaration is the exact mechanism that causes duplicate React/Three instances (RESEARCH.md Pitfall 6)."
    - "`turbo run build --filter=@gruporeacciona/graph-renderer-three` succeeds, resolving its project reference to graph-core (INFRA-02); a throwaway fetch(...) or axios import added and reverted empirically proves the D-07 guard fires (INFRA-03)."
  artifacts:
    - packages/graph-renderer-three/package.json
    - packages/graph-renderer-three/tsconfig.json
    - packages/graph-renderer-three/src/index.ts
  key_links:
    - "graph-renderer-three's tsconfig.json references ../graph-core only, matching docs/03-architecture.md's dependency direction"
    - "graph-renderer-three's peerDependencies use catalog: references from pnpm-workspace.yaml (Plan 01-01), the same catalog react-knowledge-graph (01-06) will also reference — the single mechanism preventing version drift between the two"
---

<objective>
Fully scaffold `graph-renderer-three` — the only package (besides react-knowledge-graph) permitted to depend on Three.js/R3F — with its peerDependencies contract wired through the pnpm workspace catalog, and empirically prove the D-07 ESLint guard fires for it. This is Slice 2 of RESEARCH.md's sequencing.

Purpose: Make INFRA-07's peerDependencies contract concretely correct for the package where a plain-`dependencies` mistake would first surface as duplicate Three.js instances, and prove INFRA-03's D-07 guard (backend calls blocked, React/Three explicitly allowed) is wired correctly.
Output: `packages/graph-renderer-three/package.json`, `packages/graph-renderer-three/tsconfig.json`, `packages/graph-renderer-three/src/index.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-CONTEXT.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-RESEARCH.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-PATTERNS.md
@./.claude/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Package legitimacy check for `three` before pinning as a peerDependency</name>
  <files>none (verification-only checkpoint, no files modified)</files>
  <what-built>Nothing yet — pre-install confirmation gate per the Package Legitimacy Gate protocol.</what-built>
  <action>Present RESEARCH.md's Package Legitimacy Audit verdict for `three@0.185.1` to the human operator and wait for explicit confirmation before proceeding to Task 2's install step.</action>
  <how-to-verify>
    01-RESEARCH.md's Package Legitimacy Audit flags `three@0.185.1` as `[SUS]` (`too-new` — published 2026-07-01, 7 days before the research date), with ~11.1M weekly downloads and the long-established `github.com/mrdoob/three.js` repo. Not a legitimacy concern on adoption grounds, but the protocol requires human confirmation before pinning.
    1. Visit https://www.npmjs.com/package/three and confirm the latest version (0.185.1 or newer) and publish history look consistent — no anomalous single-release spike, no maintainer-account change flagged by npm.
    2. If anything looks anomalous, pin one minor version behind instead and note the substitution in your SUMMARY (also re-check `@react-three/fiber`'s stated peer range `three >=0.156` and `@react-three/drei`'s `three >=0.159` still accept the fallback version).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with three@0.185.1, or specify a fallback version.</resume-signal>
  <verify>
    <human-check>Human operator explicitly types "approved" or names a substitute version before Task 2 proceeds.</human-check>
  </verify>
  <done>Human operator has explicitly confirmed (or substituted) the `three` version before any install occurs.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold packages/graph-renderer-three with peerDependencies</name>
  <files>packages/graph-renderer-three/package.json, packages/graph-renderer-three/tsconfig.json, packages/graph-renderer-three/src/index.ts</files>
  <read_first>
    - 01-PATTERNS.md lines 296-317 (peerDependencies pattern — exact catalog: block)
    - 01-PATTERNS.md lines 152-160 (react-knowledge-graph tsconfig example — jsx: react-jsx pattern, adapt for this package)
    - 01-RESEARCH.md lines 286-316 (Pattern 1 — pnpm workspace catalogs)
    - 01-RESEARCH.md line 367 (Anti-Pattern: declaring react/three as plain dependencies instead of peerDependencies)
  </read_first>
  <action>
    Create `packages/graph-renderer-three/package.json`: `"name": "@gruporeacciona/graph-renderer-three"`, `"version": "0.0.0"`, `"private": true`, `"type": "module"`, `"main"/"types"` pointing at `./dist`, scripts `build`/`lint`/`typecheck` matching graph-core's pattern, `"dependencies": { "@gruporeacciona/graph-core": "workspace:*" }`, and `"peerDependencies"` set to `catalog:` for exactly `react`, `react-dom`, `three`, `@react-three/fiber`, `@react-three/drei` (per PATTERNS.md's exact block) — use `pnpm --filter @gruporeacciona/graph-renderer-three add --save-peer react react-dom three @react-three/fiber @react-three/drei` first (writes to `peerDependencies`+`devDependencies` together per RESEARCH.md), then manually replace each version string with `catalog:` to match the workspace catalog protocol (`pnpm add` alone does not know to use `catalog:` unless invoked with pnpm's catalog-aware flag — verify the exact current flag/behavior in the installed pnpm version, and fall back to a manual edit if the CLI resolves a literal version string instead).

    Create `packages/graph-renderer-three/tsconfig.json` extending `../../tsconfig.base.json`, with `compilerOptions: { jsx: "react-jsx", outDir: "./dist", rootDir: "./src", tsBuildInfoFile: "./dist/.tsbuildinfo" }`, `include: ["src"]`, `references: [{ "path": "../graph-core" }]`.

    Create `packages/graph-renderer-three/src/index.ts` with a minimal placeholder export (comment noting the real R3F scene/camera/selection logic lands in Phase 3, VIEWER-01..08) — no Three.js/R3F logic yet, just enough for `tsc --build` to succeed.
  </action>
  <acceptance_criteria>
    - `cat packages/graph-renderer-three/package.json` shows `peerDependencies` (not `dependencies`) containing all five of `react`, `react-dom`, `three`, `@react-three/fiber`, `@react-three/drei`, each set to `"catalog:"`.
    - `pnpm exec turbo run build --filter=@gruporeacciona/graph-renderer-three` exits 0.
    - `pnpm why three` (run from repo root after install) shows exactly one resolved version of `three` in the tree.
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm exec turbo run build --filter=@gruporeacciona/graph-renderer-three && node -e "const p=require('./packages/graph-renderer-three/package.json'); const peers=['react','react-dom','three','@react-three/fiber','@react-three/drei']; if(!p.peerDependencies || peers.some(d=>p.peerDependencies[d]!=='catalog:')) process.exit(1)"</automated>
  </verify>
  <done>graph-renderer-three builds cleanly, has all five peerDependencies wired via catalog:, references graph-core only, and no duplicate three/react resolution exists in the workspace tree.</done>
</task>

<task type="auto">
  <name>Task 3: Empirically verify the D-07 architecture guard against graph-renderer-three</name>
  <files>packages/graph-renderer-three/src/index.ts (temporary edits only, reverted before completion)</files>
  <read_first>
    - eslint.config.js (Plan 01-02) — the D-07 block specifically
  </read_first>
  <action>
    Confirm `react`, `three`, and `@react-three/fiber` imports are all ALLOWED here (add each temporarily, lint should pass — this is the opposite check from graph-core, proving D-07 does not over-block legitimate framework usage). Then temporarily add a bare `fetch('http://example.com');` call, run `pnpm exec eslint packages/graph-renderer-three`, confirm it fails with `no-restricted-globals`. Remove all throwaway lines, leaving only Task 2's placeholder content, and confirm a final clean lint pass.
  </action>
  <acceptance_criteria>
    - `import * as THREE from 'three'` and `import { Canvas } from '@react-three/fiber'` do NOT trigger any eslint error when temporarily added.
    - The `fetch(...)` trial fails with `no-restricted-globals`.
    - Final state matches Task 2's placeholder exactly; `pnpm exec eslint packages/graph-renderer-three` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint packages/graph-renderer-three</automated>
  </verify>
  <done>D-07 confirmed to allow react/three/@react-three/* and block fetch, then reverted to a clean state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `three`/`@react-three/*` peerDependencies | First point in the monorepo where Three.js/R3F enters the dependency tree |
| graph-renderer-three ⇄ examples/* (future consumer) | peerDependency mis-declaration here surfaces as duplicate Three.js instances only once a real consumer installs this package (Plan 01-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Tampering | `three`/@react-three/* peerDependency pin | medium | mitigate | Task 1 checkpoint (package legitimacy) before pinning; catalog: protocol ensures a single, auditable version source |
| T-01-11 | Tampering | Duplicate React/Three instances from misdeclared dependency type | high | mitigate | peerDependencies (not dependencies) declared via catalog:, per RESEARCH.md Pitfall 6; empirically checked via `pnpm why three` in Task 2 |
</threat_model>

<verification>
- `turbo run build --filter=@gruporeacciona/graph-renderer-three` succeeds.
- peerDependencies (not dependencies) declared for all five React/Three packages via catalog:.
- D-07 guard empirically confirmed to allow react/three/@react-three/* and block fetch.
</verification>

<success_criteria>
graph-renderer-three exists as a buildable package correctly positioned as the sole gateway to Three.js/R3F, with peerDependencies wired to prevent duplicate instances once consumed downstream.
</success_criteria>

## Artifacts this phase produces

- `packages/graph-renderer-three/package.json` — peerDeps via catalog:, workspace dep on graph-core
- `packages/graph-renderer-three/tsconfig.json` — jsx: react-jsx, references graph-core
- `packages/graph-renderer-three/src/index.ts` — placeholder entry point

<output>
Create `.planning/phases/01-repo-scaffolding-compliance-gates/01-04-SUMMARY.md` when done
</output>

