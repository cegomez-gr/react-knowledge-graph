---
phase: 01-repo-scaffolding-compliance-gates
plan: 06
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - packages/react-knowledge-graph/package.json
  - packages/react-knowledge-graph/tsconfig.json
  - packages/react-knowledge-graph/src/index.ts
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-07]

must_haves:
  truths:
    - "packages/react-knowledge-graph/package.json declares react/react-dom/three/@react-three/fiber/@react-three/drei as peerDependencies via catalog: (INFRA-07), references both graph-core and graph-renderer-three in its tsconfig.json (INFRA-02)."
    - "A throwaway direct `import * as THREE from 'three'` (or `@react-three/fiber`) added to react-knowledge-graph's source fails lint under D-06 (Three.js access must route through graph-renderer-three, never directly from the public layer) — while `import React from 'react'` is explicitly ALLOWED (D-06 does not block react/react-dom, unlike D-05)."
  artifacts:
    - packages/react-knowledge-graph/package.json
    - packages/react-knowledge-graph/tsconfig.json
    - packages/react-knowledge-graph/src/index.ts
  key_links:
    - "react-knowledge-graph's tsconfig.json references both ../graph-core and ../graph-renderer-three, matching docs/03-architecture.md's dependency direction (this is the public entry point, depends on both lower layers)"
    - "react-knowledge-graph's package.json name @gruporeacciona/react-knowledge-graph is the exact target package examples/basic-usage (Plan 01-07) installs as a real workspace:* dependency to prove INFRA-07's no-duplicate-instance criterion end-to-end"
---

<objective>
Fully scaffold `react-knowledge-graph` — the public component layer and the package that will eventually be published as `@gruporeacciona/react-knowledge-graph` — with its peerDependencies contract and the D-06 architecture guard (allows react/react-dom, blocks direct Three.js access and all backend calls). This is Slice 3 of RESEARCH.md's sequencing, and the final package before the integration-proof slice (examples/*).

Purpose: Prove INFRA-07's peerDependencies contract at the exact layer README.md's target usage snippet (`import { KnowledgeGraphViewer } from '@gruporeacciona/react-knowledge-graph'`) names, and prove INFRA-03's D-06 guard correctly distinguishes "React is fine here" from "Three.js must route through graph-renderer-three, never imported directly."
Output: `packages/react-knowledge-graph/package.json`, `packages/react-knowledge-graph/tsconfig.json`, `packages/react-knowledge-graph/src/index.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-CONTEXT.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-RESEARCH.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-PATTERNS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/react-knowledge-graph with peerDependencies</name>
  <files>packages/react-knowledge-graph/package.json, packages/react-knowledge-graph/tsconfig.json, packages/react-knowledge-graph/src/index.ts</files>
  <read_first>
    - 01-PATTERNS.md lines 152-160 (react-knowledge-graph tsconfig.json — exact example, jsx + dual references)
    - 01-PATTERNS.md lines 296-317 (peerDependencies pattern, catalog: block)
    - README.md lines 25-42 (target public usage snippet — the exact import path and prop shape this package must eventually expose, though no component logic is written this phase)
    - 01-CONTEXT.md line 28 (D-06 exact rule text)
  </read_first>
  <action>
    Create `packages/react-knowledge-graph/package.json`: `"name": "@gruporeacciona/react-knowledge-graph"` (the exact public package name from README.md/PROJECT.md), `"version": "0.0.0"`, `"private": true`, `"type": "module"`, scripts `build`/`lint`/`typecheck`, `"dependencies": { "@gruporeacciona/graph-core": "workspace:*", "@gruporeacciona/graph-renderer-three": "workspace:*" }`, and `"peerDependencies"` set to `catalog:` for `react`, `react-dom`, `three`, `@react-three/fiber`, `@react-three/drei` (same five as graph-renderer-three, Plan 01-04 — needed because this package re-exports/wraps graph-renderer-three's R3F tree and its own peer consumers need the full set resolved consistently). Use `pnpm --filter @gruporeacciona/react-knowledge-graph add --save-peer react react-dom three @react-three/fiber @react-three/drei` then convert each version string to `catalog:` (same procedure as Plan 01-04 Task 2).

    Create `packages/react-knowledge-graph/tsconfig.json` extending `../../tsconfig.base.json`, `compilerOptions: { jsx: "react-jsx", outDir: "./dist", rootDir: "./src", tsBuildInfoFile: "./dist/.tsbuildinfo" }`, `include: ["src"]`, `references: [{ "path": "../graph-core" }, { "path": "../graph-renderer-three" }]` (exact example from PATTERNS.md).

    Create `packages/react-knowledge-graph/src/index.ts` with a minimal placeholder export (comment noting the real `KnowledgeGraphViewer` component and hooks land in Phase 3, VIEWER-01..10) — no component logic yet, just enough for `tsc --build` to succeed.
  </action>
  <acceptance_criteria>
    - `cat packages/react-knowledge-graph/package.json` shows name `@gruporeacciona/react-knowledge-graph`, `peerDependencies` (not `dependencies`) for all five React/Three packages set to `"catalog:"`, and `dependencies` on both `@gruporeacciona/graph-core` and `@gruporeacciona/graph-renderer-three` via `workspace:*`.
    - `pnpm exec turbo run build --filter=@gruporeacciona/react-knowledge-graph` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm exec turbo run build --filter=@gruporeacciona/react-knowledge-graph && node -e "const p=require('./packages/react-knowledge-graph/package.json'); const peers=['react','react-dom','three','@react-three/fiber','@react-three/drei']; if(!p.peerDependencies || peers.some(d=>p.peerDependencies[d]!=='catalog:')) process.exit(1); if(!p.dependencies||!p.dependencies['@gruporeacciona/graph-core']||!p.dependencies['@gruporeacciona/graph-renderer-three']) process.exit(1)"</automated>
  </verify>
  <done>react-knowledge-graph builds cleanly, references both upstream packages, and has all five peerDependencies wired via catalog:.</done>
</task>

<task type="auto">
  <name>Task 2: Empirically verify the D-06 architecture guard against react-knowledge-graph</name>
  <files>packages/react-knowledge-graph/src/index.ts (temporary edits only, reverted before completion)</files>
  <read_first>
    - eslint.config.js (Plan 01-02) — the D-06 block specifically
  </read_first>
  <action>
    Confirm `import React from 'react'` (or `import { useState } from 'react'`) does NOT trigger any eslint error when temporarily added — this is the exact distinction from D-05 (graph-core blocks react entirely; react-knowledge-graph must not). Then temporarily add `import * as THREE from 'three'`, run `pnpm exec eslint packages/react-knowledge-graph`, confirm it fails with `no-restricted-imports` naming `three`. Then temporarily add `import { Canvas } from '@react-three/fiber'`, confirm it also fails (matches the `@react-three/*` pattern group). Then temporarily add a bare `fetch('http://example.com')`, confirm it fails with `no-restricted-globals`. Remove all throwaway lines and confirm a final clean lint pass identical to Task 1's placeholder.
  </action>
  <acceptance_criteria>
    - `import React from 'react'` produces zero eslint errors when temporarily added.
    - `import * as THREE from 'three'` and `import { Canvas } from '@react-three/fiber'` both fail with `no-restricted-imports`.
    - The `fetch(...)` trial fails with `no-restricted-globals`.
    - Final state matches Task 1's placeholder exactly; `pnpm exec eslint packages/react-knowledge-graph` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint packages/react-knowledge-graph</automated>
  </verify>
  <done>D-06 confirmed to allow react/react-dom while blocking direct three/@react-three/* imports and fetch, then reverted to a clean state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| react-knowledge-graph (public API) ⇄ consuming applications | This is the package that will eventually be published under `@gruporeacciona/react-knowledge-graph`; its dependency boundary is the most externally visible one in the whole monorepo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Tampering | Public layer bypassing graph-renderer-three to import Three.js directly | high | mitigate | D-06 ESLint block + this plan's Task 2 empirical verification |
| T-01-15 | Tampering | Duplicate React/Three instances from misdeclared peerDependencies at the most externally-visible package | high | mitigate | peerDependencies via catalog:, same mechanism as Plan 01-04, verified via `pnpm why react`/`pnpm why three` |
</threat_model>

<verification>
- `turbo run build --filter=@gruporeacciona/react-knowledge-graph` succeeds.
- peerDependencies wired via catalog: for all five React/Three packages; dependencies on both graph-core and graph-renderer-three via workspace:*.
- D-06 guard empirically confirmed: react/react-dom allowed, three/@react-three/*/fetch blocked.
</verification>

<success_criteria>
react-knowledge-graph exists as a buildable public-layer package with the correct dependency direction and peerDependencies contract, ready for examples/* to consume as a real workspace dependency (Plan 01-07).
</success_criteria>

## Artifacts this phase produces

- `packages/react-knowledge-graph/package.json` — public package identity, peerDeps via catalog:, deps on graph-core + graph-renderer-three
- `packages/react-knowledge-graph/tsconfig.json` — jsx: react-jsx, references both upstream packages
- `packages/react-knowledge-graph/src/index.ts` — placeholder entry point

<output>
Create `.planning/phases/01-repo-scaffolding-compliance-gates/01-06-SUMMARY.md` when done
</output>
