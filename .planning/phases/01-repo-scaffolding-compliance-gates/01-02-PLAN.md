---
phase: 01-repo-scaffolding-compliance-gates
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - eslint.config.js
  - .github/workflows/ci.yml
  - NOTICE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [INFRA-03, INFRA-04, INFRA-05]

must_haves:
  truths:
    - "eslint.config.js contains four scoped flat-config blocks matching D-05 (graph-core: blocks react/react-dom/three/@react-three/*/fetch/axios/useQuery), D-06 (react-knowledge-graph: allows react/react-dom, blocks three/@react-three/*/fetch/axios/useQuery), D-07 (graph-renderer-three: allows react/three/@react-three/*, blocks fetch/axios/useQuery), and D-08 (adapters/*: blocks react/react-dom/three/@react-three/*) — each block pairs `no-restricted-imports` with `no-restricted-globals` for `fetch` per RESEARCH.md Pitfall 1 (fetch is a global, not an import)."
    - "NOTICE.md contains the literal SHA f0c9be19c5d74b84f418d807bfdce7b5d6a261ff, the tag v0.8.1, the full inline MIT license text, and 'Copyright (c) 2025 DeusData' — zero remaining placeholder phrases from the original file (INFRA-05, covered per specless-probe edge item, category unclassified)."
    - "`.planning/PROJECT.md`'s Key Decisions table row for the source commit pin now states the concrete SHA/tag (D-04) instead of the prior vague 'confirmado vía GitHub API' text."
    - { statement: "INFRA-03's ESLint architecture guard behaves correctly under interrupted or parallel CI execution (e.g. two PRs linting concurrently)", verification: backstop }
  artifacts:
    - eslint.config.js
    - .github/workflows/ci.yml
    - NOTICE.md (finalized)
    - .planning/PROJECT.md (Key Decisions table row updated)
  key_links:
    - "eslint.config.js's `files` globs (packages/graph-core/**, packages/react-knowledge-graph/**, packages/graph-renderer-three/**, packages/adapters/**) are matched against directories created in Plans 01-03..01-06 — each of those plans' acceptance criteria empirically re-verifies its own D-0X block against this file, without editing it again"
    - ".github/workflows/ci.yml's license-compliance step consumes `license-checker-rseidelsohn` installed as a root devDependency in Plan 01-01"
  prohibitions:
    - statement: "MUST NOT allow a PR to bypass a failing architecture-boundary or license-compliance CI gate by disabling/weakening the guard itself (removing an eslint.config.js block, loosening the --failOn SPDX list, adding an eslint-disable comment) instead of fixing the underlying violation — the guardrail's purpose is structural enforcement, not a checkbox to route around."
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
    - statement: "MUST NOT include any npm/pnpm publish step, publish token, or auto-publish CI trigger in .github/workflows/ci.yml or any package.json script this phase creates — public/npm-scope publication under @gruporeacciona requires prior internal Grupo Reacciona approval (organization conduct policy, PUB-04 out of scope), not a decision this phase's CI scaffolding can make."
      category: values
      status: resolved
      verification: test
      resolution: null
      reason: null
---

<objective>
Author the four per-package ESLint architecture-boundary guards (D-05..D-08), wire the CI pipeline that runs Turborepo tasks plus the license-compliance gate (D-13), and finalize the two legal-attribution artifacts that currently carry placeholder text (D-01..D-04). This closes INFRA-03, INFRA-04, and INFRA-05 in one cross-cutting "repo guardrails" slice — none of this is package-specific logic, all of it is root-level tooling per RESEARCH.md's Architectural Responsibility Map ("Repo tooling (root)").

Purpose: Make the roadmap's Success Criteria #2 ("a PR that imports React/Three/fetch into graph-core or react-knowledge-graph fails lint in CI") and #3 ("a PR with a GPL/AGPL/SSPL dependency is blocked in CI") and #4 (NOTICE.md finalized, no placeholders) mechanically true before any package or feature code exists.
Output: `eslint.config.js`, `.github/workflows/ci.yml`, finalized `NOTICE.md`, updated `.planning/PROJECT.md` Key Decisions table.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-CONTEXT.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-RESEARCH.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-PATTERNS.md
@docs/adr/0003-no-internal-fetching.md
@NOTICE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ESLint architecture-boundary guard (D-05..D-08)</name>
  <files>eslint.config.js</files>
  <read_first>
    - 01-PATTERNS.md lines 168-239 ("eslint.config.js (D-05 through D-08...)" — full reference implementation, verbatim rule content)
    - 01-CONTEXT.md lines 23-32 (the exact per-package D-05..D-08 rule text, in the user's own words)
    - 01-RESEARCH.md lines 382-389 (Pitfall 1: `no-restricted-imports` cannot catch `fetch()`)
    - 01-RESEARCH.md lines 405-410 (Pitfall 4: nested-adapter typed-linting glob-array hazard — use `projectService: true`)
  </read_first>
  <action>
    Create `eslint.config.js` at repo root exactly per PATTERNS.md's "Full reference implementation" block: `js.configs.recommended` + `tseslint.configs.recommended` as the base, an `ignores` block for `**/dist/**`/`**/node_modules/**`, then four scoped blocks matched by `files` globs:
    - `packages/graph-core/**/*.{ts,tsx}` (D-05): `no-restricted-imports` blocking `react`, `react-dom`, `three`, the `@react-three/*` pattern group, `axios`, and `@tanstack/react-query`'s `useQuery`/`useMutation` import names; `no-restricted-globals` blocking `fetch`.
    - `packages/react-knowledge-graph/**/*.{ts,tsx}` (D-06): same backend-call block as above, `no-restricted-imports` blocking `three` and the `@react-three/*` pattern group only (NOT `react`/`react-dom` — this package legitimately imports React); `no-restricted-globals` blocking `fetch`.
    - `packages/graph-renderer-three/**/*.{ts,tsx}` (D-07): only the backend-call `no-restricted-imports` block (react/three/@react-three/* are all permitted here); `no-restricted-globals` blocking `fetch`.
    - `packages/adapters/**/*.{ts,tsx}` (D-08): `no-restricted-imports` blocking `react`, `react-dom`, `three`, and the `@react-three/*` pattern group (no backend-call block needed here per PATTERNS.md's exact example — adapters are pure conversion functions with no HTTP concern stated in D-08).
    Set `tseslint`'s typed-linting via `parserOptions.projectService: true` (not a manual `project: [...]` glob array) so `packages/adapters/codebase-memory/tsconfig.json` — one directory level deeper than `packages/*` — is not silently skipped (RESEARCH.md Pitfall 4).
  </action>
  <acceptance_criteria>
    - `eslint.config.js` contains exactly four `files`-scoped blocks matching the four globs above.
    - Every block that must catch `fetch` (D-05, D-06, D-07) contains both `no-restricted-imports` AND `no-restricted-globals` — grep confirms `no-restricted-globals` appears at least 3 times.
    - `grep -c 'projectService' eslint.config.js` is >= 1 and the older glob-array form `parserOptions.project: [` does not appear.
    - `pnpm exec eslint --print-config eslint.config.js` (or equivalent) parses without a syntax error — this is verifiable even before any package directory exists, since ESLint flat config is valid JS regardless of whether the `files` globs currently match anything.
  </acceptance_criteria>
  <verify>
    <automated>node --check eslint.config.js && grep -c "no-restricted-globals" eslint.config.js | grep -qv "^0$" && grep -q "projectService" eslint.config.js</automated>
  </verify>
  <done>eslint.config.js exists with all four D-05..D-08 blocks, `no-restricted-globals` present in every block requiring the fetch-global guard, and `projectService: true` used instead of a manual glob array.</done>
</task>

<task type="auto">
  <name>Task 2: CI pipeline — install, Turbo tasks, license-compliance gate (D-13)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - 01-PATTERNS.md lines 261-292 (".github/workflows/ci.yml" pattern assignment, full YAML example)
    - 01-CONTEXT.md line 47 (D-13: `--failOn` deny-list, GPL/AGPL/SSPL only, MPL/LGPL/EPL is manual-review not a CI block)
    - 01-RESEARCH.md lines 391-403 (Pitfall 2: `pnpm licenses list` cannot fail a build; Pitfall 3: exact-SPDX enumeration, not bare prefixes, to avoid conflating GPL/LGPL)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` per PATTERNS.md's exact example: trigger on `push` to `main` and `pull_request`; a single `build` job on `ubuntu-latest` running `actions/checkout@v4`, `pnpm/action-setup@v3`, `actions/setup-node@v4` (node-version 24, cache pnpm), `pnpm install --frozen-lockfile`, `pnpm exec turbo run build lint typecheck`, then a named "License compliance gate" step running `pnpm dlx license-checker-rseidelsohn --production=false --excludePrivatePackages --failOn` with the exact enumerated SPDX deny-list from PATTERNS.md/RESEARCH.md: `"GPL-1.0-only;GPL-1.0-or-later;GPL-2.0-only;GPL-2.0-or-later;GPL-3.0-only;GPL-3.0-or-later;AGPL-1.0-only;AGPL-3.0-only;AGPL-3.0-or-later;SSPL-1.0"` — deliberately NOT a bare `"GPL"`/`"LGPL"` prefix (Pitfall 3) and NOT `--onlyAllow` (which would collapse D-13's manual-review tier into the blocked tier).

    Also rewrite root `package.json`'s `licenses:check` script (currently the placeholder `"echo 'Add pnpm licenses list once packages are implemented'"` from Plan 01-01) to invoke the same `license-checker-rseidelsohn --failOn "<same SPDX list>"` command, so `pnpm run licenses:check` is runnable locally, matching the CI step.

    Do not add any `npm publish`/`pnpm publish` step, `NPM_TOKEN` env var, or publish trigger to this workflow — per this plan's `must_haves.prohibitions`, publication requires prior Reacciona approval and is out of scope for Phase 1's CI.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists, triggers on `push:branches:[main]` and `pull_request`, and contains the literal string `pnpm exec turbo run build lint typecheck`.
    - The license-compliance step's `--failOn` value contains no bare `"GPL"` or `"LGPL"` token as a standalone list entry (only fully-qualified SPDX identifiers).
    - `grep -riE "npm publish|pnpm publish|NPM_TOKEN"  .github/workflows/ci.yml package.json` returns no matches (prohibition P2 empirically checkable now).
    - Root `package.json`'s `licenses:check` script no longer contains the word `echo`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "turbo run build lint typecheck" .github/workflows/ci.yml && grep -q "failOn" .github/workflows/ci.yml && ! grep -riE "npm publish|pnpm publish|NPM_TOKEN" .github/workflows/ci.yml package.json</automated>
  </verify>
  <done>.github/workflows/ci.yml exists with install → turbo run → license gate steps, the SPDX deny-list is exact/enumerated (no bare GPL/LGPL prefix), root package.json's licenses:check script is wired to the real tool, and no publish step exists anywhere in this phase's tooling.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize legal attribution (D-01..D-04)</name>
  <files>NOTICE.md, .planning/PROJECT.md</files>
  <read_first>
    - NOTICE.md (current placeholder, full file already in context — note its own instruction: "Before publishing, replace this notice with the exact upstream repository URL, copyright holder and license text from the specific revision used.")
    - 01-PATTERNS.md lines 105-115 ("NOTICE.md (finalize — edit, not replace)")
    - 01-CONTEXT.md lines 16-22 (D-01..D-04: exact SHA/tag/copyright/license text and the PROJECT.md update requirement)
    - .planning/PROJECT.md (Key Decisions table, row: "Commit fuente fijado: tag v0.8.1...")
  </read_first>
  <action>
    Edit `NOTICE.md`, keeping its existing structural shape (short prose intro + fenced attribution block), replacing the placeholder with: the exact upstream repository `github.com/DeusData/codebase-memory-mcp`, the pinned tag `v0.8.1`, the commit SHA `f0c9be19c5d74b84f418d807bfdce7b5d6a261ff` (published 2026-06-12), the copyright line `Copyright (c) 2025 DeusData`, and the FULL MIT license text inline (not a reference to a separate file — D-02). Add an explicit sentence per D-03 stating this pin is provisional and MUST be re-verified against `codebase-memory-mcp`'s latest release before any code is copied in Phase 3 — phrase it as a standing instruction to the Phase 3 executor, not as a settled permanent fact.

    Edit `.planning/PROJECT.md`'s Key Decisions table: update the row currently reading "Commit fuente fijado: tag v0.8.1 (SHA f0c9be19c5d74b84f418d807bfdce7b5d6a261ff, 2026-06-12), copyright (c) 2025 DeusData" — change its Outcome column from "— Pending (re-verificar en Fase 3)" to reflect that Phase 1 has now finalized `NOTICE.md` with this exact pin (keep the "re-verificar en Fase 3" caveat, since D-03 is still binding for Phase 3, but mark the Phase 1 NOTICE.md-finalization portion as done).
  </action>
  <acceptance_criteria>
    - `grep -c "f0c9be19c5d74b84f418d807bfdce7b5d6a261ff" NOTICE.md` >= 1.
    - `grep -c "v0.8.1" NOTICE.md` >= 1 and `grep -c "DeusData" NOTICE.md` >= 1.
    - `grep -qi "MIT License" NOTICE.md` matches (full license text present, not a reference).
    - `grep -qi "replace this notice with the exact upstream" NOTICE.md` returns NO match (the original placeholder sentence must be gone).
    - `.planning/PROJECT.md`'s Key Decisions table row for the source commit pin no longer reads only "— Pending (re-verificar en Fase 3)" without qualification of Phase 1 completion.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "f0c9be19c5d74b84f418d807bfdce7b5d6a261ff" NOTICE.md && grep -qi "MIT License" NOTICE.md && ! grep -qi "replace this notice with the exact upstream" NOTICE.md</automated>
  </verify>
  <done>NOTICE.md contains the real SHA/tag/copyright/full MIT text with zero placeholder language, and PROJECT.md's Key Decisions table reflects D-04's update.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → CI guardrails | Any contributor's PR is the untrusted input this plan's guardrails (eslint.config.js, license gate) evaluate |
| Third-party GitHub Actions → CI runner | `actions/checkout@v4`, `pnpm/action-setup@v3`, `actions/setup-node@v4` execute with repository-scoped permissions inside the CI runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | eslint.config.js / CI license gate (guardrail integrity) | high | mitigate | This is the exact threat named in the phase's security_contribution: a PR could disable an eslint.config.js block or loosen `--failOn` instead of fixing a real violation. Mitigated structurally by: (a) the guard lives in a single reviewed file whose diff is visible in any PR that touches it, (b) `must_haves.prohibitions` P1 (this plan) makes "must not weaken the guard" an explicit, judgment-tier acceptance criterion future code review must check, (c) CODEOWNERS-style review is a process control outside this phase's automatable scope — flagged, not silently assumed solved. |
| T-01-05 | Tampering | GPL/AGPL/SSPL entering the dependency tree transitively | high | mitigate | `license-checker-rseidelsohn --failOn` with the exact enumerated SPDX deny-list (not `pnpm licenses list`, which has no fail-on-violation capability per RESEARCH.md Pitfall 2); Pitfall 3's exact-string enumeration avoids substring conflation between GPL and LGPL |
| T-01-06 | Tampering | Third-party GitHub Action tampered after being referenced by a movable tag (`@v3`/`@v4` repointed) | medium | accept | GitHub-owned actions (`actions/checkout`, `actions/setup-node`) are lower risk than third-party ones; SHA-pinning is a reasonable hardening step but not required to satisfy INFRA-03/04 — accepted as a follow-up hardening item, not blocking this phase (RESEARCH.md Security Domain, V14) |
| T-01-07 | Repudiation | Lockfile drift between local install and CI | medium | mitigate | `pnpm install --frozen-lockfile` in the CI step fails the build instead of silently re-resolving if `pnpm-lock.yaml` is out of sync |
| T-01-08 | Information Disclosure | Accidental publication under `@gruporeacciona` scope before internal approval | high | mitigate | must_haves.prohibitions P2 (this plan) — no publish step/token in this phase's CI or scripts, verified via grep in Task 2's acceptance criteria |
</threat_model>

<verification>
- `eslint.config.js` parses and contains all four D-05..D-08 blocks with `no-restricted-globals` for fetch where required.
- `.github/workflows/ci.yml` wires install → turbo run build/lint/typecheck → license-compliance gate with an exact enumerated SPDX deny-list, and contains no publish step.
- `NOTICE.md` has zero placeholder language and the real SHA/tag/copyright/MIT text.
- `.planning/PROJECT.md`'s Key Decisions table reflects D-04.
</verification>

<success_criteria>
Roadmap Success Criteria #2 (lint blocks bad imports/fetch), #3 (CI blocks GPL/AGPL/SSPL), and #4 (NOTICE.md finalized) are mechanically wired and ready for empirical verification once real packages exist (Plans 01-03 onward) and once real dependencies exist (Plan 01-08's scratch-branch license test).
</success_criteria>

## Spec-less Probe Coverage Disposition

Per the specless-probe-fallback edge coverage report supplied for this phase (9 applicable items across INFRA-01..07):

- **Covered** (defensible acceptance criteria authored into `must_haves.truths` across this plan set): INFRA-01, INFRA-02 (Plan 01-01); INFRA-05 (this plan); INFRA-04/adjacency and INFRA-04/empty (Plan 01-08, empirical scratch-branch test); INFRA-06, INFRA-07 (Plan 01-08).
- **Unresolved / flagged as genuinely-inapplicable** (per the explicit planning-context hint that concurrency/adjacency/ordering probes may not meaningfully apply to config/scaffolding requirements):
  - **INFRA-03 / concurrency** ("if interrupted or run in parallel, what is guaranteed?") — does not meaningfully apply: `eslint.config.js` is a static, stateless rule-set with no interruptible operation to reason about. Two CI jobs linting in parallel across different PRs share no mutable state. Flagged, not resolved into a truth.
  - **INFRA-04 / ordering** ("when elements compare equal, is output order specified and stable?") — does not meaningfully apply: the license gate is a set-membership check against a blocklist (pass/fail), not a sorted or ranked output whose element ordering could be ambiguous. Flagged, not resolved into a truth.
- **Prohibitions** (two-stage recall/precision run per `prohibition-probe.md`, descriptor-less per D-06/RAIL-04): P1 (guardrail-bypass, judgment-tier) and P2 (no-publish-step, test-tier) — both authored into this plan's `must_haves.prohibitions` above, both `status: resolved`.

No item from the coverage report was silently dropped.

## Artifacts this phase produces

- `eslint.config.js` — root flat-config, four D-05..D-08 scoped blocks
- `.github/workflows/ci.yml` — install/build/lint/typecheck/license-gate pipeline
- `NOTICE.md` (finalized) — real SHA/tag/copyright/full MIT text
- `.planning/PROJECT.md` — Key Decisions table row updated (D-04)
- `package.json` (root) — `licenses:check` script rewired to the real tool

<output>
Create `.planning/phases/01-repo-scaffolding-compliance-gates/01-02-SUMMARY.md` when done
</output>
