---
phase: 01-repo-scaffolding-compliance-gates
plan: 05
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - packages/adapters/codebase-memory/package.json
  - packages/adapters/codebase-memory/tsconfig.json
  - packages/adapters/codebase-memory/src/index.ts
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03]

must_haves:
  truths:
    - "Only packages/adapters/codebase-memory exists — no packages/adapters/graphology or packages/adapters/neo4j directory is created in this phase (D-09 explicitly overrides ROADMAP.md's literal three-adapter listing; graphology/neo4j are Milestone 6, v2, out of scope)."
    - "packages/adapters/codebase-memory contains only package.json + tsconfig.json + a placeholder entry point — no fromCodebaseMemory() implementation or any adapter logic exists yet (D-10); `turbo run build --filter` succeeds and a throwaway react/three import empirically fails the D-08 guard."
  artifacts:
    - packages/adapters/codebase-memory/package.json
    - packages/adapters/codebase-memory/tsconfig.json
    - packages/adapters/codebase-memory/src/index.ts
  key_links:
    - "packages/adapters/codebase-memory/tsconfig.json references ../../graph-core only (types, not implementation) — adapters must never be imported by react-knowledge-graph per docs/03-architecture.md"
---

<objective>
Scaffold the ONE adapter package this phase creates — `packages/adapters/codebase-memory` — per D-09/D-10's explicit YAGNI override of ROADMAP.md's literal three-adapter listing, and empirically prove the D-08 architecture guard fires against it. No adapter logic is written this phase (that's Milestone 6, ADAPT-01, v2, out of scope).

Purpose: Give Phase 3 (VIEWER-01) a package directory to eventually place the real `fromCodebaseMemory()` conversion function in, without prematurely scaffolding `graphology`/`neo4j` adapters that have no v1 requirement backing them.
Output: `packages/adapters/codebase-memory/package.json`, `packages/adapters/codebase-memory/tsconfig.json`, `packages/adapters/codebase-memory/src/index.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-CONTEXT.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-RESEARCH.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/adapters/codebase-memory (D-09/D-10 — only this one, no logic)</name>
  <files>packages/adapters/codebase-memory/package.json, packages/adapters/codebase-memory/tsconfig.json, packages/adapters/codebase-memory/src/index.ts</files>
  <read_first>
    - 01-CONTEXT.md lines 34-41 (D-09, D-10 — exact scope: only codebase-memory, package.json+tsconfig.json only, no logic)
    - 01-RESEARCH.md lines 422-424 (Pitfall 7: recreating v2-only adapter stubs "for completeness" is a genuine, easy-to-make mistake — do not do it)
    - 01-PATTERNS.md lines 42-51 (pnpm-workspace.yaml directory names — "ONLY this one in Phase 1")
    - 01-PATTERNS.md lines 224-233 (eslint.config.js D-08 block — adapters/* restrictions)
  </read_first>
  <action>
    Verify `packages/adapters/graphology/` and `packages/adapters/neo4j/` do NOT exist before or after this task (D-09) — do not create them under any circumstance, even though ROADMAP.md's literal Phase 1 success-criterion text names all three.

    Create `packages/adapters/codebase-memory/package.json`: `"name": "@gruporeacciona/adapter-codebase-memory"` (D-12 scope; singular "adapter" — this exact name is Claude's Discretion since no locked decision specifies the literal npm name, only the directory path `adapters/codebase-memory`; document this choice in the SUMMARY), `"version": "0.0.0"`, `"private": true`, `"type": "module"`, scripts `build`/`lint`/`typecheck` matching the same pattern as graph-core, `"dependencies": { "@gruporeacciona/graph-core": "workspace:*" }` (types only, per D-10 — no actual usage yet). Declare zero `react`/`react-dom`/`three`/`@react-three/*` dependencies (D-08).

    Create `packages/adapters/codebase-memory/tsconfig.json` extending `../../../tsconfig.base.json` (one directory level deeper than the other packages — verify the relative path is correct: `packages/adapters/codebase-memory/` to repo root is three levels up), `compilerOptions: { outDir: "./dist", rootDir: "./src", tsBuildInfoFile: "./dist/.tsbuildinfo" }`, `include: ["src"]`, `references: [{ "path": "../../graph-core" }]`.

    Create `packages/adapters/codebase-memory/src/index.ts` with only a placeholder comment noting the real `fromCodebaseMemory()` conversion function is Milestone 6 / ADAPT-01 (v2, deferred) — per D-10, no logic, "not even a stub of fromCodebaseMemory()". The placeholder exists solely to satisfy TypeScript's composite-build requirement for a non-empty `include: ["src"]` config (an empty `src/` directory errors with "No inputs were found").
  </action>
  <acceptance_criteria>
    - `ls packages/adapters/` shows exactly one directory: `codebase-memory`. `test -d packages/adapters/graphology` and `test -d packages/adapters/neo4j` both fail (directories do not exist).
    - `cat packages/adapters/codebase-memory/package.json` shows zero `react`/`react-dom`/`three`/`@react-three/*` keys.
    - `grep -c "fromCodebaseMemory" packages/adapters/codebase-memory/src/index.ts` — if present, it appears only inside a comment, not as an executable function declaration (grep for `function fromCodebaseMemory` or `export function fromCodebaseMemory` returns zero matches).
    - `pnpm exec turbo run build --filter=@gruporeacciona/adapter-codebase-memory` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm exec turbo run build --filter=@gruporeacciona/adapter-codebase-memory && ! test -d packages/adapters/graphology && ! test -d packages/adapters/neo4j && ! grep -q "export function fromCodebaseMemory" packages/adapters/codebase-memory/src/index.ts</automated>
  </verify>
  <done>packages/adapters/codebase-memory is the only adapter package created, builds cleanly, and contains no adapter logic — only scaffolding.</done>
</task>

<task type="auto">
  <name>Task 2: Empirically verify the D-08 architecture guard against adapters/codebase-memory</name>
  <files>packages/adapters/codebase-memory/src/index.ts (temporary edits only, reverted before completion)</files>
  <read_first>
    - eslint.config.js (Plan 01-02) — the D-08 block specifically
    - 01-RESEARCH.md lines 405-410 (Pitfall 4 — confirm projectService: true correctly reaches this nested package, one level deeper than packages/*)
  </read_first>
  <action>
    Temporarily add a bare `import 'react';` line to `packages/adapters/codebase-memory/src/index.ts`, run `pnpm exec eslint packages/adapters/codebase-memory`, confirm it fails with `no-restricted-imports` naming `react`. Remove it, then temporarily add a bare `import 'three';` line, run the same lint command, and confirm it also fails with `no-restricted-imports` naming `three` — mirroring the two-trial pattern used in Plans 01-03/01-04/01-06 so D-08's three-blocking half (not just its react-blocking half) is empirically proven, not merely inferred from the shared eslint.config.js block. Remove it, then confirm typed linting actually reaches this file at all (not silently skipped due to the Pitfall 4 nested-glob hazard) by checking `pnpm exec eslint packages/adapters/codebase-memory --debug 2>&1 | grep -i "codebase-memory"` shows the file was processed, or equivalently confirm a deliberately-introduced TypeScript type error in this file is caught by `pnpm exec tsc --noEmit` when run scoped to this package. Revert all throwaway edits and confirm a final clean lint pass.
  </action>
  <acceptance_criteria>
    - The `react` import trial fails eslint with `no-restricted-imports`.
    - The `three` import trial fails eslint with `no-restricted-imports`.
    - Typed linting is confirmed to actually process files under `packages/adapters/codebase-memory/` (not silently skipped).
    - Final state matches Task 1's placeholder exactly; `pnpm exec eslint packages/adapters/codebase-memory` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint packages/adapters/codebase-memory</automated>
  </verify>
  <done>D-08 guard confirmed working (both react-blocking and three-blocking halves) against the nested adapters/codebase-memory path, with typed linting confirmed reachable (Pitfall 4 avoided), then reverted to clean state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapters/codebase-memory ⇄ future external MCP data (Milestone 6) | Not yet a real boundary this phase — no conversion logic exists — but the package identity/scope is established now |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Tampering | Scope creep — creating graphology/neo4j adapter stubs against D-09 | medium | mitigate | Task 1's explicit negative-existence check (`! test -d packages/adapters/graphology`) in automated verify, citing RESEARCH.md Pitfall 7 |
| T-01-13 | Tampering | Nested adapter package silently skipped by typed linting (Pitfall 4) | medium | mitigate | `projectService: true` in eslint.config.js (Plan 01-02) + this plan's Task 2 empirical confirmation that linting reaches the nested path |
</threat_model>

<verification>
- Exactly one adapter package exists: `packages/adapters/codebase-memory`.
- It builds via Turborepo/tsc, contains zero adapter logic, and the D-08 guard is empirically confirmed to fire correctly against its nested path.
</verification>

<success_criteria>
The adapters/codebase-memory package exists as pure scaffolding — ready for Milestone 6's real adapter logic — without any premature graphology/neo4j stub creation.
</success_criteria>

## Artifacts this phase produces

- `packages/adapters/codebase-memory/package.json` — `@gruporeacciona/adapter-codebase-memory`, zero UI deps, workspace dep on graph-core
- `packages/adapters/codebase-memory/tsconfig.json` — extends root base, references graph-core
- `packages/adapters/codebase-memory/src/index.ts` — placeholder entry point (no adapter logic)

<output>
Create `.planning/phases/01-repo-scaffolding-compliance-gates/01-05-SUMMARY.md` when done
</output>

