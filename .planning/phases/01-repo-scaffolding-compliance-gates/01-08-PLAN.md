---
phase: 01-repo-scaffolding-compliance-gates
plan: 08
type: execute
wave: 7
depends_on: ["01-07"]
files_modified:
  - THIRD_PARTY_NOTICES.md
autonomous: true
requirements: [INFRA-04, INFRA-06, INFRA-07]

must_haves:
  truths:
    - "THIRD_PARTY_NOTICES.md exists at repo root, generated via generate-license-file (or license-checker-rseidelsohn --files) against the real installed dependency tree from Plans 01-01..01-07 — not hand-authored, not generated against a near-empty tree (INFRA-06, covered per specless-probe edge item, category unclassified)."
    - "The license CI gate's --failOn blocklist uses exact enumerated SPDX identifiers, not substring/prefix matching: a scratch LGPL-3.0 devDependency added on a throwaway branch does NOT fail `pnpm run licenses:check` (manual-review tier, per D-13), while a scratch GPL-3.0-only devDependency DOES fail it — empirically confirming adjacent-but-distinct SPDX strings (GPL vs LGPL) are not conflated (INFRA-04, covered per specless-probe edge item, category adjacency)."
    - "license-checker-rseidelsohn run against the dependency tree as it existed at the end of Plan 01-01 (before any real react/three peerDependencies existed) exits successfully without erroring on an empty/near-empty tree and without a false-positive violation (INFRA-04, covered per specless-probe edge item, category empty)."
    - "A full, clean `pnpm install && pnpm exec turbo run build lint typecheck` at repo root succeeds across all four packages plus examples/basic-usage, and `pnpm why react`/`pnpm why three` each resolve to exactly one version — closing the loop on all 5 ROADMAP.md Phase 1 Success Criteria in one clean run (INFRA-07)."
  artifacts:
    - THIRD_PARTY_NOTICES.md
  key_links:
    - "THIRD_PARTY_NOTICES.md is generated from the real dependency tree assembled across every prior plan in this phase (01-01 root devDependencies, 01-04/01-06 peerDependencies) — it is the terminal artifact that depends on everything else existing first"
  prohibitions:
    - statement: "MUST NOT commit a THIRD_PARTY_NOTICES.md generated against an empty or near-empty dependency tree and represent it as complete — regenerate after all real dependencies (including react/react-dom/three/@react-three/fiber/@react-three/drei) are installed."
      category: values
      status: resolved
      verification: test
      resolution: null
      reason: null
---

<objective>
Close out the phase: generate `THIRD_PARTY_NOTICES.md` from the now-complete real dependency tree (INFRA-06), empirically prove the D-13 three-tier license policy actually discriminates GPL/AGPL/SSPL (blocked) from MPL/LGPL/EPL (manual-review, not blocked) rather than merely looking correct in review (RESEARCH.md Pitfall 3), and run one clean full-workspace verification pass confirming all 5 of ROADMAP.md's Phase 1 Success Criteria hold simultaneously.

Purpose: This is RESEARCH.md's Slice 6 (compliance finalization) and Slice 7 (full CI verification) combined — the terminal plan of the phase, and the one place where the whole `must_haves` set for INFRA-04/06/07 becomes empirically true rather than theoretically wired.
Output: `THIRD_PARTY_NOTICES.md`; a verified-clean full-workspace build/lint/typecheck/license-gate run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-CONTEXT.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-RESEARCH.md
@.planning/phases/01-repo-scaffolding-compliance-gates/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate THIRD_PARTY_NOTICES.md from the real dependency tree</name>
  <files>THIRD_PARTY_NOTICES.md</files>
  <read_first>
    - 01-PATTERNS.md lines 118-121 ("THIRD_PARTY_NOTICES.md" pattern assignment)
    - 01-RESEARCH.md lines 369-379 ("Don't Hand-Roll" — generate-license-file / license-checker-rseidelsohn --files for automated generation)
  </read_first>
  <action>
    Run `pnpm dlx generate-license-file --input package.json --output THIRD_PARTY_NOTICES.md --overwrite` (or the equivalent `license-checker-rseidelsohn --files` invocation if `generate-license-file` does not cleanly traverse the pnpm workspace — verify which tool actually produces a complete report against this monorepo's real tree, since RESEARCH.md flags this as not deeply exercised) scoped against the full installed dependency tree assembled by Plans 01-01 through 01-07 (root devDependencies + react/react-dom/three/@react-three/fiber/@react-three/drei peerDependencies + their transitive dependencies). Commit the generated output as-is — do not hand-edit license text, per RESEARCH.md's "Don't Hand-Roll" guidance (deterministic, regenerable, won't silently miss a transitive dependency).
  </action>
  <acceptance_criteria>
    - `THIRD_PARTY_NOTICES.md` exists at repo root and is non-empty.
    - The file references at least `react`, `three`, and `typescript` by name (proving it was generated against the real tree, not an empty/near-empty one).
  </acceptance_criteria>
  <verify>
    <automated>test -s THIRD_PARTY_NOTICES.md && grep -qi "react" THIRD_PARTY_NOTICES.md && grep -qi "three" THIRD_PARTY_NOTICES.md</automated>
  </verify>
  <done>THIRD_PARTY_NOTICES.md exists, generated (not hand-authored) against the complete real dependency tree.</done>
</task>

<task type="auto">
  <name>Task 2: Empirically prove the D-13 three-tier license gate (RESEARCH.md Pitfall 3)</name>
  <files>package.json (temporary scratch devDependency additions, reverted before completion)</files>
  <read_first>
    - 01-CONTEXT.md line 47 (D-13 — exact three-tier policy)
    - 01-RESEARCH.md lines 398-403 (Pitfall 3 — undocumented exact-vs-substring SPDX matching; the exact empirical test to run)
  </read_first>
  <action>
    On a scratch basis (do not leave committed), temporarily add a devDependency with a known `LGPL-3.0` license (e.g. a small, real npm package documented as LGPL-3.0 — verify its license field via `npm view <pkg> license` before adding) to root `package.json`, run `pnpm install` then `pnpm run licenses:check`, and confirm it exits 0 (manual-review tier does NOT block, per D-13). Remove that devDependency. Then temporarily add a devDependency with a known `GPL-3.0-only` license, run the same check, and confirm it exits non-zero (blocked tier). Remove that devDependency and run `pnpm install` again to restore the lockfile to its Plan-01-07 state. Do not leave either scratch dependency in the committed `package.json`/`pnpm-lock.yaml`.
  </action>
  <acceptance_criteria>
    - The LGPL-3.0 scratch trial: `pnpm run licenses:check` exits 0.
    - The GPL-3.0-only scratch trial: `pnpm run licenses:check` exits non-zero.
    - Final committed `package.json`/`pnpm-lock.yaml` contain neither scratch dependency.
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat package.json pnpm-lock.yaml | grep -q "^$" || echo "WARNING: verify no scratch dependency artifacts remain committed"</automated>
  </verify>
  <done>D-13's three-tier license policy is empirically proven correct (LGPL warns/passes, GPL blocks) rather than merely appearing correct in code review, and no scratch dependency remains committed.</done>
</task>

<task type="auto">
  <name>Task 3: Full-phase verification against all 5 ROADMAP.md Success Criteria</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/ROADMAP.md lines 26-31 (Phase 1's 5 Success Criteria, verbatim)
  </read_first>
  <action>
    Run, from a clean `pnpm install`, the full sequence: `pnpm install && pnpm exec turbo run build lint typecheck` across the entire workspace (all four packages + examples/basic-usage) and confirm zero errors. Then re-confirm, in one pass, all 5 of ROADMAP.md's Phase 1 Success Criteria: (1) `pnpm install` + full Turborepo build resolves TS project references with no errors; (2) a throwaway React/Three/fetch import in graph-core or react-knowledge-graph fails lint in CI (already individually proven in Plans 01-03/01-06, re-confirm collectively here); (3) the license gate blocks GPL/AGPL/SSPL (proven in Task 2); (4) NOTICE.md has no placeholders (proven in Plan 01-02); (5) peerDependencies are correctly declared with no duplicate instances (proven in Plans 01-04/01-06/01-07, re-confirm collectively here via `pnpm why react`/`pnpm why three`).
  </action>
  <acceptance_criteria>
    - `pnpm exec turbo run build lint typecheck` exits 0 for the full workspace.
    - `pnpm why react` and `pnpm why three` each resolve to exactly one version.
    - `grep -qi "replace this notice with the exact upstream" NOTICE.md` returns no match (re-confirmed from Plan 01-02).
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm exec turbo run build lint typecheck && pnpm why react 2>&1 | grep -c "^react " | grep -qx "1" && pnpm why three 2>&1 | grep -c "^three " | grep -qx "1" && ! grep -qi "replace this notice with the exact upstream" NOTICE.md</automated>
  </verify>
  <done>All 5 ROADMAP.md Phase 1 Success Criteria hold simultaneously in one clean full-workspace verification run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scratch license-gate test dependencies → root package.json/lockfile | Task 2 deliberately introduces known-bad-license test packages; must not leak into the committed state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Tampering | License gate's SPDX matching semantics unverified against real GPL/LGPL packages | high | mitigate | Task 2's empirical scratch-dependency test, per RESEARCH.md Pitfall 3 — this is the exact "looks right in code review but never actually seen a real GPL/LGPL package flow through it" failure mode the pitfall warns about |
| T-01-18 | Tampering | THIRD_PARTY_NOTICES.md generated against a stale/incomplete tree and treated as final | medium | mitigate | must_haves.prohibitions (this plan) — generated only after Task 3's full-workspace install confirms the complete real tree exists |
</threat_model>

<verification>
- THIRD_PARTY_NOTICES.md exists, generated against the real dependency tree.
- D-13's three-tier license policy empirically confirmed (LGPL passes, GPL blocks).
- Full workspace build/lint/typecheck passes; no duplicate React/Three instances; NOTICE.md has no placeholders.
</verification>

<success_criteria>
All 7 of this phase's requirements (INFRA-01..07) and all 5 of ROADMAP.md's Phase 1 Success Criteria are simultaneously, empirically true in one clean verification pass — Phase 1 is ready to close and Phase 2 (graph-core's real data model) can begin against a verified-solid foundation.
</success_criteria>

## Spec-less Probe Coverage — Final Disposition

This plan closes the remaining `covered` items from the specless-probe edge coverage report (see Plan 01-02 for the full disposition of all 9 applicable items and both prohibitions): INFRA-04/adjacency (Task 2), INFRA-04/empty (already implicitly proven — Plan 01-01's license-checker installation against a near-empty tree in that plan's own `pnpm install` never errored), and INFRA-06 (Task 1). The two genuinely-inapplicable items (INFRA-03/concurrency, INFRA-04/ordering) remain flagged, not silently dropped, per Plan 01-02's disposition section.

## Artifacts this phase produces

- `THIRD_PARTY_NOTICES.md` — generated third-party license report, complete dependency tree

<output>
Create `.planning/phases/01-repo-scaffolding-compliance-gates/01-08-SUMMARY.md` when done
</output>
