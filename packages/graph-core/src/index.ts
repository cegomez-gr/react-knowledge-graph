// graph-core: neutral graph data model (GraphNode, GraphEdge, NormalizedGraph)
// Real types/validators/normalizers land in Phase 2 (CORE-01). This is a
// placeholder entry point so the package has a valid, non-empty compilation
// unit for `tsc --build` during Phase 1 scaffolding.
export {};
