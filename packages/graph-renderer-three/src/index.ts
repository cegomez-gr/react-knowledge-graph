// graph-renderer-three: Three.js / React Three Fiber rendering layer.
// Real scene/camera/selection/animation logic lands in Phase 3 (VIEWER-01..08).
// This is a placeholder entry point so the package has a valid, non-empty
// compilation unit for `tsc --build` during Phase 1 scaffolding.
export {};
